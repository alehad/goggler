## REMOVED Requirements

### Requirement: Purchases analytics summary

**Reason**: Averaging, min, and max paid price across dissimilar won items is not a meaningful figure regardless of currency, and the underlying aggregation ignored currency entirely when items spanned more than one currency. Removed rather than fixed; each won item continues to show its own price on its own card and chart point.
