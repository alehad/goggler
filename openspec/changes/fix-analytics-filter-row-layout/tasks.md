# Tasks: Fix Analytics filter row layout

- [x] Create OpenSpec change documenting the root cause and fix.
- [x] Confirm the bug and the fix visually via an isolated reproduction using the real `app/styles.css`, at both narrow and wide viewports.
- [x] First attempt: wrap the two bulk-action buttons in `<div className="section-heading-actions">`. Shipped for review, but the user caught a regression (buttons wrapped onto their own column instead of sitting on the same line as the filter controls) — see design.md's "v1 (wrong)" section.
- [x] Second attempt: merge the buttons into `.filter-row` with a scoped `padding: 0 6px` override on segmented-control buttons. Measured 38px of margin in an isolated repro and shipped — but the repro's placeholder buttons omitted the real `<Check>`/`<Trash2>` lucide icons (~24px per button of icon+gap not accounted for), so it still didn't fit in the real app and the user caught it a second time from a live screenshot ("Delete all visible" wrapping to its own line).
- [x] Final fix, per the user's explicit spec (one line, same width as the search box/list, all elements same height and vertically centered, smaller font permitted): rebuilt the row as a dedicated compact bar — `font-size: 12px`, `min-height: 36px` on both the segmented-control buttons' container math and the action buttons (tuned so both sides land on exactly 36px including padding/border, not just eyeballed), `padding: 3px`/`0 8px`/`0 9px` tightened throughout. This time verified against the *exact* real `app/styles.css` with real icon-sized placeholder elements (16px + button gap, matching lucide's rendered footprint) via `getBoundingClientRect()`: all four elements measure exactly 36px tall, top-aligned to the same pixel, with 51px of real width margin to spare. Also re-checked graceful wrapping at a 420px viewport.
- [x] `npx tsc --noEmit`, `npm run build` (dev server stopped first to avoid corrupting its `.next` cache — see [[analytics-ai-assistant]]'s prior incident), `npm run test:unit` (195 passing) — all clean. Dev server restarted cleanly afterward.
- [x] Follow-up polish (requested after the layout itself was confirmed working): right-align the two bulk-action buttons to the search box's right edge, keeping the filter controls left-aligned — wrapped the buttons in a new `.filter-row-actions` div with `margin-left: auto`. Verified with `getBoundingClientRect()`: the actions group's right edge matches the search box's right edge exactly (0px difference).
- [ ] Manual visual confirmation on the real Analytics tab (requires an eBay session).
- [ ] Run dual security review (security-review skill + Copilot CLI), then ship via PR.
