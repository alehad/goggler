## MODIFIED Requirements

### Requirement: Matching preferences controls
The UI SHALL expose matching preference controls under My goggler.

#### Scenario: Generic default criteria
- **WHEN** the matching preferences are first loaded
- **THEN** the criteria field SHALL include a generic record-ID pattern for one to five letters followed immediately by one to six digits
- **AND** the field SHALL remain editable by the user
