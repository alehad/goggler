# Tasks: macOS Purchases tab

- [x] Create OpenSpec change documenting the design.
- [x] Wait for user sign-off on this design before implementing.
- [x] Implement `PurchasesView` (search, purchase rows, count, Refresh), wired into `ContentView`'s `detailView` switch.
- [x] `xcodebuild build` and `xcodebuild test` clean (8/8).
- [ ] Manual functional confirmation (user): Purchases shows real won items, search filters correctly by title/seller.
- [ ] Run dual security review (security-review skill + Copilot CLI), then ship via PR.
