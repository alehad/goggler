# Tasks: Persistent local backend via a launchd LaunchAgent

- [x] Create OpenSpec change documenting the design (plist shape, why launchd over Docker/sidecar, the standalone-output note, verification approach).
- [x] Wait for user sign-off on this design before implementing.
- [x] Add `~/Library/LaunchAgents/com.goggler.server.plist` (not tracked in the repo — it's machine-local config, referencing an absolute path on this Mac).
- [x] `npm run build` once to produce a production build for the LaunchAgent to serve. (Discovered `next start` warns it's incompatible with `output: standalone`; switched to running `node .next/standalone/server.js` directly, matching the Dockerfile's own runtime. This needed two follow-on fixes: copying `.next/static` into the standalone output (missing by default), and sourcing `.env.local` explicitly in the LaunchAgent's command, since standalone output doesn't do Next's usual automatic dotenv loading the way `next dev`/`next start` do.)
- [x] `launchctl bootstrap`/`launchctl kickstart` the agent, confirm the server comes up and serves correctly, confirm logs land in `~/Library/Logs/goggler-server.log`.
- [x] Confirm `KeepAlive` actually restarts the process after a manual `kill`. (Verified: killed the process, a new PID came up within ~3 seconds, server responded 200 again.)
- [x] Add the "keep the backend up" section to `AGENTS.md`, including the one-time Tailscale "Open at Login" step.
- [x] Manual functional confirmation (user): launch the macOS app fresh and confirm the startup gate passes straight through with no Retry needed.
- [x] Manual functional confirmation (user): full cold-state simulation — stopped the LaunchAgent (`launchctl bootout`) and disconnected Tailscale (`tailscale down`) together, confirmed both endpoints unreachable, confirmed the macOS app correctly showed the "Can't reach goggler" overlay, then brought both back the way login would (`tailscale up` + `launchctl bootstrap`), confirmed reachability recovered with zero manual `npm run dev`/`tailscale serve` commands, and confirmed a fresh app launch goes straight into the sidebar with no gate delay. A true logout/reboot test was intentionally not forced (would have killed the active session); this cold-start simulation is an accurate proxy for the same login-time triggers.
- [x] Run dual security review (security-review skill + Copilot CLI) on the `AGENTS.md`/OpenSpec diff. The internal review found a real MEDIUM finding (8/10 confidence): the documented approach shelled out to `source .env.local` to load env vars, which executes the file as real shell script rather than parsing it as inert `KEY=VALUE` data — unlike every other env-loading path already in this repo (Docker's `--env-file`, Next's own dotenv loader). Fixed by switching to Node's native `--env-file` flag (`node --env-file=.env.local .next/standalone/server.js`, no shell involved), re-verified the same env-dependent endpoint still loads correctly, and updated `AGENTS.md`/`design.md` to match.
- [ ] No PR needed for the plist itself (machine-local, not repo-tracked) — but the `AGENTS.md` update ships via the normal PR flow.
